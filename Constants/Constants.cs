namespace NotARealCompanyWithViews.Constants
{
    public static class Constants
    {
        // Replace with the actual base URL if needed
        public const string API_BASE_URL = "https://api.example.com/";
        public const string API_REQUEST_URL = "sales";
        public const string LOCAL_JSON_PATH = "Sample/sales.json";

        // Set to false to fetch from API instead of local file
        public const bool IS_LOCAL = true;
    }
}
